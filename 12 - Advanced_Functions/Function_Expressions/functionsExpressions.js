function add(x, y) {
    return x + y
}
// é a mesma coisa que eu fazer isso e colocar numa variável.
// other syntax
// functions are objects

const sum = function (x, y) {
    return x + y
}
// MESMA COISA
const soma = function somar() {
    return x + y
}
console.dir(add) // inspect na função   f 